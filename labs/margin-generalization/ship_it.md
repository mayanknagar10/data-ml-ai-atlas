# Ship it

Version scaling, feature map, kernel, C, weights and calibration; validate margin and error distributions on later cohorts, monitor score and support drift, and roll back preprocessing and classifier together.
