# Ship it

Version the stream and hash schema, reject incompatible merges, track candidates separately, audit estimates against exact samples, protect counters from overflow, and rotate or roll back complete epochs.
