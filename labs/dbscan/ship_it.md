# Ship it

Freeze metric and density semantics, benchmark neighbor memory, define new-point core assignment, monitor core and noise rates, protect reference data, and retain a support fallback and prior model.
