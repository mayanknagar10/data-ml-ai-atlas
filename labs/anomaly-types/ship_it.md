# Ship it

Version unit, contexts, reference window, robust statistics, aggregation and threshold; enforce past-only features, deduplicate incidents, monitor context coverage and alert budget, quarantine reference updates, and fall back to safe deterministic rules when context is unavailable.
