# Ship it

Freeze transform, covariance and regularization; repeat starts; monitor likelihood, weight and eigenvalue health; align component identities; retain a broad-density fallback and prior mixture.
