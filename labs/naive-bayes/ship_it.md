# Ship it

Persist vocabulary, smoothing, priors and counts; test log-space parity and unknown inputs; monitor priors, feature drift and calibration; protect raw content and retain a prior-only fallback.
