# Ship it

Version population, sampling unit, labels, loss, weights, class, features, split and optimizer; validate on untouched grouped or temporal cohorts, monitor risk and drift by slice, and roll back the complete objective-plus-model artifact.
