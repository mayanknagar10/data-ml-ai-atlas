# Ship it

Version analyzers, dictionaries, intent and entity models, schemas and rewrite thresholds; replay component and downstream slices, shadow transformations with provenance, enforce privacy and expansion limits, and retain original-query fallback and prior rules for rollback.
