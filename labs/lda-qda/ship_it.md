# Ship it

Freeze feature units, class order, priors, covariance and solver; monitor conditioning, distance, prevalence and calibration; flag unsupported points and retain a simpler covariance fallback.
