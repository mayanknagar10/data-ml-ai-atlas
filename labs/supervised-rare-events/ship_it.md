# Ship it

Persist event and maturity definitions, splits, inclusion probabilities, weights, model, calibration and threshold separately; audit nonalerts, monitor population/sample prevalence and queue outcomes, and roll back the entire sampled-training-to-policy bundle.
