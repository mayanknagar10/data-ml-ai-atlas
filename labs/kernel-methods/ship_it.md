# Ship it

Freeze kernel geometry, solver and any approximation map; benchmark quadratic resources and tail latency; monitor similarity support and quality; protect reference data and retain a linear fallback.
