# Ship it

Version variables, alphabet or bins, estimator, scaling, neighbor or critic settings and sampling unit; run selection only inside training folds, report uncertainty and shuffled nulls, and avoid silent comparison across incompatible estimators.
