# Ship it

Turn this LLM Training Lifecycle: Pretraining → SFT → Preference Tuning reference into a versioned component with explicit schemas, deterministic tests, slice-aware evaluation, monitoring, resource limits, and a rollback path.
