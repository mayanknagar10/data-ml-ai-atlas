# Ship it

Turn this Change Data Capture (CDC) reference into a versioned component with explicit schemas, deterministic tests, slice-aware evaluation, monitoring, resource limits, and a rollback path.
