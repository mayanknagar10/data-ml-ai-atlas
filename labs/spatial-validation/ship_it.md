# Ship it

Freeze deployment-aligned spatial and temporal manifests, keep preprocessing inside folds, report support and distance diagnostics, reserve an untouched region, and define out-of-coverage fallback.
