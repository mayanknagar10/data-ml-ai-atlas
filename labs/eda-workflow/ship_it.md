# Ship it

Run profiles on versioned snapshots, preserve grain and lineage, fail on key/range/reconciliation violations, monitor trends by source/time, and redact sensitive or rare values from artifacts.
