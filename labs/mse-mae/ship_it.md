# Ship it

Freeze target, unit, weight and aggregation semantics; test metric invariants; monitor label coverage, tails and slices with independent-unit uncertainty; retain the prior scorecard and model for rollback.
