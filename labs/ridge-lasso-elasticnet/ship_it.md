# Ship it

Persist preprocessing statistics, exact objective convention, alpha, l1 ratio, solver and feature order; reject schema drift, track convergence and coefficient-path stability, ablate active dependencies, and keep the previous pipeline for canary rollback.
