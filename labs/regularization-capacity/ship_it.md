# Ship it

Version scaling, penalty, lambda, optimizer, augmentation and stopping; tune inside grouped or temporal validation, monitor norms, stability and later-cohort utility, and roll back the complete preprocessing and learner configuration.
