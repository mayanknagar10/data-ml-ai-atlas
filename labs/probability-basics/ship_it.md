# Ship it

Use audited prevalence and outcome sampling, stable calibrated probabilities, explicit cost/capacity thresholds, slice monitoring, sensitive-data controls and a versioned model-threshold rollback bundle.
