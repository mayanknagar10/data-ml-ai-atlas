# Ship it

Freeze preprocessing and hash-family versions, size signatures from labeled threshold error, index candidates separately, exact-check ambiguous or destructive merges, and monitor signature and cluster drift.
