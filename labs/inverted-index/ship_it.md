# Ship it

Version source IDs, schema and analyzer; build immutable segments, reconcile counts and sampled postings, enforce permissions before exposure, monitor refresh and merge health, and switch aliases only after replay validation with a rollback snapshot.
