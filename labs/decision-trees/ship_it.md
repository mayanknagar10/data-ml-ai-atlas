# Ship it

Put encoders inside validation, version split and pruning settings, reject schema violations, monitor leaf occupancy and delayed calibration, and retain a baseline and previous artifact for rollback.
