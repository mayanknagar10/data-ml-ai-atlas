# Ship it

Persist an immutable trial ledger, model failures and constraints, version the surrogate/acquisition/space, cap total cost and concurrency, monitor calibration and regret against random search, and retain the previous safe recommendation for rollback.
