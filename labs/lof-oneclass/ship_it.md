# Ship it

Persist scaler, metric, reference, k or kernel/gamma/nu, score direction and threshold; version neighbor indexes or support vectors, test novelty semantics, monitor distance and support drift, and roll back model plus reference together.
