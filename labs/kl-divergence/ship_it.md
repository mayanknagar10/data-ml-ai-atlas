# Ship it

Version direction, alphabet, normalization, smoothing, estimator, window and units; publish contribution and ratio diagnostics, monitor support and tail drift, and never merge incompatible divergence series silently.
