# Ship it

Version both indexes, analyzers, embeddings, IDs, filters, branch depths, timeouts and fusion; evaluate branch and union recall plus final ranking and latency, shadow compatible generations, and preserve single-branch safe fallbacks and atomic rollback.
