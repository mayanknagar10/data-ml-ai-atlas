# Ship it

Version labels, logits API, weights, smoothing, masks, sampling and reduction; test extreme values and golden batches, monitor finite per-slice losses and calibration, and roll back preprocessing plus model as one compatible artifact.
