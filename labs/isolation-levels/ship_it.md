# Ship it

Implement the invariant with target-database constraints/locking or Serializable transactions, whole-transaction idempotent retries, consistent lock order, timeouts, and monitoring of abort/lock rates.
