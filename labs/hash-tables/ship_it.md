# Ship it

Add resize policy, memory and cardinality limits, stable normalization, concurrency control, metrics, and explicit retention or eviction before using a mapping as a cache or deduplication service.
