# Ship it

Record constraint signs, units, status, active set, dual gap, and scaled KKT residuals; validate shadow prices with perturbations; monitor multiplier instability; and use a safe feasible fallback when certification fails.
