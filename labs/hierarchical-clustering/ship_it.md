# Ship it

Freeze distance, linkage, constraints and cut; validate pairwise and merge invariants; align cluster identities across refits; monitor assignment support; retain a flat fallback and prior hierarchy.
