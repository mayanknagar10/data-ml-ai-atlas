# Ship it

Freeze metric, transform, reference cutoff and index; measure exact recall and tail latency; monitor neighbor distance and fallbacks; enforce privacy, deletion and version-compatible updates.
