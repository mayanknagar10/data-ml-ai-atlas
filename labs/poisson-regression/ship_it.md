# Ship it

Version count window, event deduplication, exposure definition and units, offset, features, family and penalty; reconcile exposure totals, monitor zero and tail calibration plus aggregate capacity error, and retain the previous full pipeline for rollback.
