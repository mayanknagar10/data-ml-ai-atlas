# Ship it

Version event-time ordering, residualization, cost, penalty, minimum segment, confirmation, reset and tolerance; join change metadata, group alarms, monitor run length and delay, and roll back detector plus baseline policy together.
