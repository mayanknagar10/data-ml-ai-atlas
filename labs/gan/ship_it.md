# Ship it

GAN inference can be fast, but training reproducibility and validation are difficult. Store checkpoints/metrics and evaluate diversity plus task-specific correctness, not only discriminator loss.
