# Ship it

Run fast checks per change, version golden datasets and artifacts, separate environment suites, record seeds and tolerances, and combine pre-release regression with canary monitoring and rollback.
