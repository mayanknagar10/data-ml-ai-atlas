# Ship it

Freeze label, weight and learner semantics; log round errors, margins and effective sample size; calibrate independently; monitor drift and latency; retain a simple fallback and prior ensemble.
