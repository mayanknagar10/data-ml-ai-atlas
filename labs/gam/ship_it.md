# Ship it

Persist per-component bases, centering vectors, penalties, link, interaction set, support, and component order; test component sums against total predictions, monitor contribution and support drift, and canary or roll back the complete GAM pipeline.
