# Ship it

Version candidate generators, depth, text selection, tokenizer, model and features; evaluate candidate ceiling and reranked metrics with tail latency, shadow the complete cascade, and retain the authorized base ranking as a monitored timeout fallback.
