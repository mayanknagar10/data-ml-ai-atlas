# Ship it

Persist fold provenance and a compatible base/meta manifest; align class and score schemas; parallelize within deadlines; monitor component drift and fallbacks; roll back the bundle atomically.
