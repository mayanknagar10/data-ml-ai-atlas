# Ship it

Use verified SciPy distribution/log-tail APIs and independent NumPy Generator streams, version parameter conventions and seeds, monitor tail/dispersion/support drift and keep a validated fallback model.
