# Ship it

Version CRS, transform, resolution, nodata and band semantics with every raster; record rasterization rules, window reads and conservation tests, and retain source geometry for reproducible rebuilds.
